apiVersion: v1
kind: Service
metadata:
  name: notification-service
spec:
  type: NodePort
  selector:
    app: notification
  ports:
    - protocol: TCP
      port: 8084
      targetPort: 8084
      nodePort: 30084  # Optional: static port, can also let Minikube assign one
